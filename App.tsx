import { StatusBar } from 'expo-status-bar'
import { NativeBaseProvider, Box } from 'native-base'

export default function App() {
  return (
    <NativeBaseProvider>
      <Box>Open up App.tsx to start working on your app!</Box>
    </NativeBaseProvider>
  )
}
