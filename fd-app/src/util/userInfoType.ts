export type userType = { id: number, email: string, role: string }

export type userLongType = {
  id: number,
  email: string,
  name: null,
  birthday: null,
  role: string,
  created_at: string,
  updated_at: string
}