import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,

    marginTop: '10%',
    width: '100%',
  },

  innerContainer: {
    width: '100%',
    alignSelf: 'center',
    alignItems: 'center',
  },
});